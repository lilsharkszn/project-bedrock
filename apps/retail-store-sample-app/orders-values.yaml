app:
  persistence:
    provider: postgresql
    endpoint: bedrock-postgres.cedysk464ef6.us-east-1.rds.amazonaws.com:5432
    database: orders
    secret:
      create: false
      name: orders-db
  messaging:
    provider: in-memory
postgresql:
  create: false
